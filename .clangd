# clangd 配置（跨平台共用）：
# - Windows：clangd 默认按 MSVC 目标解析，找不到 stdio.h，需要强制 MinGW 目标
# - macOS/Linux：使用原生 SDK，绝不能加 MinGW 目标，否则会去 mingw 目录找 stdio.h 而报错
If:
  PathMatch: ['^[A-Za-z]:']   # 仅匹配 Windows 盘符路径（C:/...、D:/...）
CompileFlags:
  Compiler: gcc
  Add:
    - --target=x86_64-w64-windows-gnu
